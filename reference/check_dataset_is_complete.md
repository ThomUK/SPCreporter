# Check all required data items are provided

Check all required data items are provided

## Usage

``` r
check_dataset_is_complete(report_config, measure_data)
```

## Arguments

- report_config:

  A data frame. The report config detailing required report items

- measure_data:

  Data frame in wide format

## Value

logical TRUE if check is successful, else an error message
